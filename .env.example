# Copy to .env and fill in. .env is gitignored.
# NVIDIA NIM (build.nvidia.com) — free tier, used for audio verification
NVIDIA_API_KEY=nvapi-...
# Hugging Face — needed for gated pyannote and DialogueSidon weights
HF_TOKEN=hf_...
