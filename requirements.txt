numpy>=1.24
scipy>=1.10
soundfile>=0.12
requests>=2.31
yt-dlp>=2024.1.0

# diarization + separation
torch>=2.7
# forced alignment uses torchaudio.functional.forced_align, which upstream has
# deprecated and announced for removal in torchaudio 2.9 -- stay below that.
torchaudio>=2.7,<2.9
pyannote.audio>=3.4,<4
diffusers>=0.35
huggingface_hub>=0.30
silero-vad>=5.0

# transcription (word-level timestamps)
# ASR: bodhan-ai/indic-transcribe-core (gated HF repo -- request access and set
# HF_TOKEN in .env); one model for all 27 Indic languages.
# Alignment (default): ai4bharat/indicwav2vec-hindi (Apache-2.0, "auto"-gated:
# click "agree" on its HF page once with the account behind HF_TOKEN) run
# through transformers + torchaudio forced_align on GPU.
transformers>=4.40
librosa>=0.10

# legacy alignment backend (ALIGN_BACKEND=onnx), no longer the default: CPU
# ONNX aligner + real uroman romanization. Only needed if you switch to it.
uroman>=1.3
ctc-forced-aligner>=1.0.2

# fallback ASR + timestamps for the rare clip whose alignment fails outright:
# gives real word timestamps for just that clip instead of interpolation.
faster-whisper>=1.0

# system requirement: ffmpeg and ffprobe on PATH
